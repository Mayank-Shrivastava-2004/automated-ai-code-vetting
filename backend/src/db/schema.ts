/**
 * db/schema.ts — no-op stub
 *
 * The in-memory store needs no DDL. This file is kept so any future
 * migration back to SQLite only requires replacing database.ts and schema.ts.
 */
export function applySchema(): void {
  // No-op: in-memory store requires no schema setup.
}
